<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ru.rerumu.lists.mappers.AuthorBookMapper">

    <delete id="deleteByAuthor">
        delete from lists.books_authors
        where author_author_id=#{authorId}
    </delete>

    <insert id="add">
        insert into lists.books_authors(book_book_id, book_list_id, author_author_id, author_list_id)
        values(#{bookId},#{readListId},#{authorId},#{readListId})
    </insert>

</mapper>